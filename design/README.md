# UI/UX-дизайн StroyControl

Визуальный дизайн ключевых экранов веб-интерфейса. Живые HTML/CSS-макеты — не
конечный продакшн-код, а дизайн-каркас (кликабельная статика) на единой
дизайн-системе для передачи фронтенд-разработке и обсуждения с заказчиком.

## Дизайн-система

`design-system.css` — цвета, типографика, отступы, компоненты (карточки,
бейджи статусов, кнопки, прогресс-бары, вкладки), фирменные иконки
(`icons.svg` — inline SVG-спрайт, без внешних шрифтов/CDN) и лёгкие
анимации (fade-in карточек, заполнение прогресс-баров, hover-эффекты,
sticky-топбар с блюром). Палитра статусов: 🟢 в графике/готово, 🟠 риск
срыва, 🔴 просрочено/срыв, ⚪ не начато — используется единообразно во всех
модулях (графики, задачи, дефект-лист).

## Экраны

Экраны кликабельно связаны между собой через боковое меню и хлебные крошки —
можно переходить между ними как в настоящем приложении.

| # | Экран | Модуль ТЗ | Файл |
|---|---|---|---|
| 1 | Список объектов, KPI-сводка | §4 Объекты и план-график | `01-objects-dashboard.html` |
| 2 | Диаграмма Ганта объекта | §4 Объекты и план-график | `02-object-gantt.html` |
| 3 | Канбан-доска задач | §5 Задачи на объекте | `03-tasks-kanban.html` |
| 4 | Фотоконтроль, дефект-лист | §6 Фотоконтроль и технадзор | `04-photocontrol.html` |
| 5 | Лента объекта | §8 Лента и коммуникации | `05-feed.html` |
| 6 | Кабинет заказчика | §12 Кабинет заказчика | `06-customer-portal.html` |

Скриншоты экранов — в `screenshots/`.

## Как открыть макеты локально

```bash
cd design && python3 -m http.server 8000
# затем открыть http://localhost:8000/01-objects-dashboard.html
```

## Адаптивность (мобильный экран / APK)

Макеты рассчитаны на просмотр и на десктопе, и на телефоне — при ширине
экрана ≤768px (`@media (max-width: 768px)` в `design-system.css`):

- Левый десктопный сайдбар скрывается, вместо него — нижняя навигация
  (`.bottom-nav`), как в обычном мобильном приложении, со своим набором
  пунктов на каждом экране (кабинет заказчика использует отдельный набор).
- Многоколоночные сетки (KPI, карточки объектов, фотогалереи, двухколоночные
  layout'ы) сворачиваются в одну колонку.
- Канбан-доска задач и диаграмма Ганта не сжимаются в нечитаемый вид, а
  скроллятся по горизонтали (свайпом) — как таблицы/доски в мобильных
  приложениях планирования.
- Второстепенные переключатели вида (Канбан/Календарь на экране Ганта)
  скрываются на телефоне — эту роль берёт на себя нижняя навигация.

Это то же самое, что видно в собранном демо-APK — `mobile/stroycontrol-demo/`
собирает именно эти файлы через Capacitor.

## Демо-приложение (APK)

`../mobile/stroycontrol-demo/` — эти же экраны, упакованные через Capacitor
в нативную Android-оболочку (WebView), для просмотра дизайна прямо на
телефоне. Это визуальное демо, не production-приложение (нет связи с
backend/API) — см. `../mobile/stroycontrol-demo/README.md`.

## Не входит в этот каркас

- Полноценное нативное мобильное приложение (раздел 13 ТЗ: офлайн-режим,
  геолокация, QR-сканер, голосовой ввод) — здесь только адаптивная вёрстка
  и WebView-обёртка, не нативный React Native код
- Экраны Этапа 2/3 (финансы, снабжение, видеонаблюдение, AI-функции)
- Реальная интерактивность (формы с валидацией, модальные окна, drag&drop)
- Финальная предметная проработка в Figma с передачей спецификаций (redlines, компонентная библиотека)
